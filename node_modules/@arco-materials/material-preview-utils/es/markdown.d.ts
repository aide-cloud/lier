import { CodeBlock } from './interface';
export declare function getCodeForRenderPackageDirectly(packageName: string): string;
export declare function extractCodeBlocks(text: string): Promise<[CodeBlock[], string]>;
export declare function generateLiveCodeReact(rawCode: string, materialPackageName: string): {
    code: string;
    unexpectImportList: string[];
};
export declare function generateLiveCodeVue(rawCode: string, materialPackageName: string): {
    code: string;
    unexpectImportList: any[];
};
