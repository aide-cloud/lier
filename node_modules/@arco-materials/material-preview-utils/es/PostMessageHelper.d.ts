import { Message, PostMessageOption } from './interface';
export default class PostMessageHelper {
    private readonly key;
    private readonly targetWindow;
    private readonly targetOrigin;
    private readonly onMessageHandler;
    constructor({ targetWindow, targetOrigin, onMessage, key, }: PostMessageOption);
    postMessage<T>(message: Message<T>): void;
    destroy(): void;
}
