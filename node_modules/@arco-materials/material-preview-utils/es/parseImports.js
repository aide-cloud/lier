import parseEsImport from 'parse-es-import';
export default function parseImports(content) {
    return parseEsImport(content).imports.map(function (result) {
        return {
            moduleName: result.moduleName,
            starImport: result.starImport,
            namedImports: result.namedImports,
            defaultImport: result.defaultImport,
            sideEffectOnly: result.sideEffectOnly,
        };
    });
}
