"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const fs_extra_1 = __importDefault(require("fs-extra"));
const path_1 = __importDefault(require("path"));
const arco_cli_dev_utils_1 = require("arco-cli-dev-utils");
const getConfigEntryDir_1 = __importDefault(require("./getConfigEntryDir"));
function getConfigProcessor(configType) {
    const configFilePath = path_1.default.resolve((0, getConfigEntryDir_1.default)(), `${configType}.config.js`);
    let processor = null;
    if (fs_extra_1.default.existsSync(configFilePath)) {
        try {
            processor = require(configFilePath);
        }
        catch (error) {
            arco_cli_dev_utils_1.print.error('[arco-doc-site]', `Failed to extend configuration from ${configFilePath}`);
            console.error(error);
            process.exit(1);
        }
    }
    return processor;
}
exports.default = getConfigProcessor;
