import { GlobConfigForBuild, MainConfig } from '../interface';
export declare type SubmodulePathInfo = Record<keyof GlobConfigForBuild, {
    glob: GlobConfigForBuild[keyof GlobConfigForBuild];
    path: string | Record<string, string>;
}>;
export default function getSubmodulePath(buildConfig: MainConfig['build'], languages: string[]): {
    pathInfoMap: Record<string, SubmodulePathInfo>;
    globsToWatch: string[];
};
