"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const path_1 = __importDefault(require("path"));
const fs_extra_1 = __importDefault(require("fs-extra"));
function getTitleOfMarkdown(filePath) {
    const docContent = fs_extra_1.default.readFileSync(filePath, 'utf-8');
    let title = path_1.default.basename(filePath).replace(/\..+$/, '');
    // try to get name from markdown content
    let _match = docContent.match(/`{5}\n(.+)`{5}\n/s);
    if (_match && _match[1]) {
        _match = _match[1].match(/#\s(.+)\n/);
        title = (_match && _match[1]) || title;
    }
    return title;
}
exports.default = getTitleOfMarkdown;
