import { ModuleUMDInfo } from '@arco-materials/material-preview-utils/es/interface';
import { ModuleExportInfoMap } from './parseModuleExport';
import { MainConfig } from '../interface';
export declare type ModuleInfo = {
    name: string;
    info: Record<string, string> & {
        umd?: ModuleUMDInfo;
    };
    isDoc?: boolean;
    children?: Array<{
        name: string;
        info: Record<string, string>;
        rawCode?: string;
    }>;
    outline: Array<{
        depth: number;
        text: string;
    }>;
};
export declare type ModuleInfoOfEntry = {
    key: string;
    doc: ModuleInfo[];
    component: ModuleInfo[];
};
declare type ChunkInfo = {
    name: string;
    entry: string;
    files?: string[];
};
export default class ArcoSiteModuleInfoPlugin {
    private paths;
    constructor(options: {
        globs?: MainConfig['build']['globs'];
    });
    /**
     * Get module info like
     * {
     *   zh-CN: [
     *     {
     *       key: 'submodule_1',
     *       components: [ { name: 'Button', info: {}, children: [] } ],
     *       documents: [ { name: 'Doc_1', info: {}, isDoc: true } ]
     *     }
     *   ]
     * }
     */
    getModuleInfoMap({ chunkInfoList, moduleExportInfoMap, }: {
        moduleExportInfoMap: ModuleExportInfoMap;
        chunkInfoList: Array<ChunkInfo>;
    }): Record<string, ModuleInfoOfEntry[]>;
    apply(compiler: any): void;
}
export {};
